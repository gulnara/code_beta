<div class = 'dashboard'>
	<% @activities.each do |activity| %>
	  <li> <%= image_tag activity.owner.avatar.url(:thumb) %><%= link_to activity.owner.name, activity.owner if activity.owner %>
	    <%= render_activity activity %>
		</li>
	<% end %>
</div>