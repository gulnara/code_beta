added a solution
<%= activity.trackable.solution.title %>

<% if activity.trackable %>
  <%= link_to activity.trackable.problem.title, activity.trackable.problem %>
<% else %>
  which has since been removed
<% end %>