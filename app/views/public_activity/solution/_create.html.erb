


<% if activity.trackable %>
  <%= link_to "added a solution to #{activity.trackable.problem.title}", problem_solution_path(activity.trackable.problem, activity.trackable)%>
<% else %>
  created a solution which has since been removed
<% end %>